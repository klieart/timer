<%= form_for(@timer) do |f| %>
  <% if @timer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timer.errors.count, "error") %> prohibited this timer from being saved:</h2>

      <ul>
      <% @timer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user2timer_id %><br>
    <%= f.text_field :user2timer_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :timer %><br>
    <%= f.number_field :timer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
